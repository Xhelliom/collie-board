id = "herdr.collie-board"
name = "Collie Board"
version = "0.120.0"
min_herdr_version = "0.7.0"
description = "Durable Kanban cards over your Herdr agent herd — worktree per card, handoffs, mobile PWA"
platforms = ["linux", "macos"]

# This plugin is a thin launcher. The actual bridge runs as a systemd --user service so it
# outlives Herdr restarts (see ARCHITECTURE.md §3). The actions below shell out to
# scripts/collie-board-ctl.sh, which gets HERDR_SOCKET_PATH / HERDR_PLUGIN_CONFIG_DIR injected.

# Build the web UI at install time. Herdr runs [[build]] steps ONLY on `herdr plugin install`
# (GitHub) — NOT on `herdr plugin link` (local dev), which instead builds lazily on first `start`
# (collie-board-ctl.sh's ensure_build). Delegating to `collie-board-ctl.sh build` keeps a single build
# definition (version gate → cd web && bun install && bun run build) shared with the `update` action.
# Still needs Bun on PATH — building the frontend is the one unavoidable Bun step.
[[build]]
command = ["bash", "scripts/collie-board-ctl.sh", "build"]
platforms = ["linux", "macos"]

# First-run bootstrap: preflight, link, and derive the two security settings from `tailscale status`
# so the safe configuration is the default one. Deliberately starts and publishes nothing.
[[actions]]
id = "setup"
title = "Set up (preflight + config)"
contexts = ["workspace"]
command = ["bash", "scripts/collie-board-ctl.sh", "setup"]

[[actions]]
id = "start"
title = "Start web bridge"
contexts = ["workspace"]
command = ["bash", "scripts/collie-board-ctl.sh", "start"]

[[actions]]
id = "stop"
title = "Stop web bridge"
contexts = ["workspace"]
command = ["bash", "scripts/collie-board-ctl.sh", "stop"]

[[actions]]
id = "restart"
title = "Restart web bridge"
contexts = ["workspace"]
command = ["bash", "scripts/collie-board-ctl.sh", "restart"]

# Full teardown: stops + disables the service, removes the systemd --user unit, and resets
# tailscale serve. Leaves the .env and the checkout (so `start` can bring it right back). See
# scripts/collie-board-ctl.sh → cmd_uninstall.
[[actions]]
id = "uninstall"
title = "Uninstall web bridge (remove service)"
contexts = ["workspace"]
command = ["bash", "scripts/collie-board-ctl.sh", "uninstall"]

# Link-mode plugins ARE their on-disk git checkout (Herdr has no native `plugin update`), so this
# action pulls + rebuilds + restarts in one step. See scripts/collie-board-ctl.sh → cmd_update.
[[actions]]
id = "update"
title = "Update plugin"
contexts = ["workspace"]
command = ["bash", "scripts/collie-board-ctl.sh", "update"]

[[actions]]
id = "url"
title = "Show bridge URL"
contexts = ["workspace"]
command = ["bash", "scripts/collie-board-ctl.sh", "url"]

[[actions]]
id = "status"
title = "Bridge status"
contexts = ["workspace"]
command = ["bash", "scripts/collie-board-ctl.sh", "status"]

[[actions]]
id = "version"
title = "Show version"
contexts = ["workspace"]
command = ["bash", "scripts/collie-board-ctl.sh", "version"]
