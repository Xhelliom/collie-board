bump: minor

### Added
- Runs (ADR 0017) : mode sélection sur un board scopé à un repo, feuille « Run » (ordre, parallélisme, plafond de fold-ins, agent du lead)
- `POST /api/runs` : enregistre le run et pose `run_id` sur les cartes choisies, sans rien démarrer
