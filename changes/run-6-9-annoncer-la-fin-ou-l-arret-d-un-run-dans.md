bump: minor

### Added
- Notification `Needs you` quand un run s'arrête sur une carte (`run.halted`), retirée dès que l'opérateur la fait bouger
- Notification `Done` quand tous les membres d'un run sont déposés (`run.finished`), retirée dès qu'un membre bouge
