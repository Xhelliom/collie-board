bump: minor

### Added
- Jauge de contexte par modèle : le dénominateur suit le modèle du pane (transcript `message.model` ou session OpenCode), table statique → models.dev → `COLLIE_BOARD_CTX_WINDOW` en repli.
- Jauge OpenCode : occupancy lue de la session db (`session_v2` + dernier message assistant), fenêtre via la même résolution.
